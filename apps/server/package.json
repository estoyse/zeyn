{
  "name": "server",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "test": "vitest run",
    "test:watch": "vitest",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server"
  },
  "dependencies": {
    "@hono/trpc-server": "^0.4.0",
    "@shaxsiy-oyin/api": "workspace:*",
    "@shaxsiy-oyin/auth": "workspace:*",
    "@shaxsiy-oyin/db": "workspace:*",
    "@shaxsiy-oyin/env": "workspace:*",
    "@trpc/server": "catalog:",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "hono": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:",
    "@shaxsiy-oyin/config": "workspace:*",
    "@types/node": "^22.13.14",
    "alchemy": "catalog:",
    "tsdown": "^0.21.9",
    "typescript": "^6",
    "vitest": "^4.1.9",
    "wrangler": "^4.77.0"
  }
}
